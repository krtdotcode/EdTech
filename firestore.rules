rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Temporarily allow all authenticated users to read/write (for development)
    // You should restrict this in production for security
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
